- hosts: [terraform-ansible]
  tasks:
    - name: Instalando o Python3, virtualenv
      apt:
        pkg:
          - python3
          - virtualenv
          - nodejs
        update_cache: true
      become: true
